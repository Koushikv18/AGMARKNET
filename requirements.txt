# AGMARKNET Pipeline — Python Dependencies
# Pin versions for reproducibility on production cluster deployments.
# Run: pip install -r requirements.txt

# --- Data acquisition ---
requests>=2.31.0          # HTTP client for data.gov.in API
tqdm>=4.66.0              # Progress bars for paginated pulls
python-dotenv>=1.0.0      # Load .env config

# --- Big data processing ---
pyspark>=3.5.0,<4.0.0   # Apache Spark Python API (3.5 compatible with Java 8/11/17)
pyarrow>=14.0.0           # Parquet read/write backend

# --- Analytics & data wrangling ---
pandas>=2.1.0             # Result CSV manipulation
numpy>=1.26.0             # Numerical operations

# --- Visualisation ---
plotly>=5.18.0            # Interactive charts (HTML output)
kaleido>=0.2.1            # Static image export from Plotly
matplotlib>=3.8.0         # Fallback static charts

# --- Geographic visualisation ---
geopandas>=0.14.0         # India state choropleth
shapely>=2.0.0            # Geometry operations (GeoJSON)
